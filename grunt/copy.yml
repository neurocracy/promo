# This copies the listed third-party libraries from their node_modules
# directories to the publicly accessible JavaScript vendor directory.
javascript:
  nonull: true
  flatten: true
  files:
    - src: 'headroom.min.js'
      cwd: 'node_modules/headroom.js/dist/'
      dest: '<%= javascriptVendorPath %>'
      expand: true
